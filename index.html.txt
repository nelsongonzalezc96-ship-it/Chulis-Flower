<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Para Chulis 🌻</title>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;500;600;700&family=Montserrat:wght@300;400;500&display=swap');

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 100%;
      min-height: 100vh;
      overflow: hidden;
      background: #07140d;
      color: #fff;
      font-family: 'Montserrat', sans-serif;
    }

    .scene {
      position: relative;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
      background:
        radial-gradient(circle at 50% 85%, rgba(91, 126, 45, 0.35), transparent 35%),
        linear-gradient(
          to bottom,
          #081a2b 0%,
          #102d3b 35%,
          #344b2e 67%,
          #0b1c0d 100%
        );
    }

    /* Luna */
    .moon {
      position: absolute;
      top: 8%;
      right: 10%;
      width: 90px;
      height: 90px;
      border-radius: 50%;
      background: #fff8d8;
      box-shadow:
        0 0 25px rgba(255, 248, 216, 0.5),
        0 0 70px rgba(255, 248, 216, 0.15);
      opacity: 0.9;
    }

    /* Estrellas */
    .star {
      position: absolute;
      width: 3px;
      height: 3px;
      border-radius: 50%;
      background: white;
      opacity: 0.5;
      animation: twinkle 3s infinite ease-in-out;
    }

    @keyframes twinkle {
      0%, 100% {
        opacity: 0.25;
        transform: scale(0.8);
      }
      50% {
        opacity: 0.9;
        transform: scale(1.3);
      }
    }

    /* Pantalla inicial */
    .start-screen {
      position: absolute;
      inset: 0;
      z-index: 100;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 25px;
      background:
        radial-gradient(circle at center, rgba(32, 67, 41, 0.4), rgba(4, 13, 8, 0.96));
      transition: opacity 1.2s ease, visibility 1.2s ease;
    }

    .start-screen.hide {
      opacity: 0;
      visibility: hidden;
      pointer-events: none;
    }

    .start-flower {
      font-size: 75px;
      margin-bottom: 20px;
      animation: floatFlower 3s ease-in-out infinite;
      filter: drop-shadow(0 0 18px rgba(255, 210, 50, 0.3));
    }

    @keyframes floatFlower {
      0%, 100% {
        transform: translateY(0) rotate(-3deg);
      }
      50% {
        transform: translateY(-10px) rotate(3deg);
      }
    }

    .start-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(38px, 8vw, 72px);
      font-weight: 500;
      color: #fff5c7;
      margin-bottom: 10px;
      letter-spacing: 1px;
    }

    .start-subtitle {
      font-size: 12px;
      letter-spacing: 5px;
      color: rgba(255,255,255,0.65);
      margin-bottom: 35px;
    }

    .open-btn {
      border: 1px solid rgba(255, 232, 150, 0.65);
      background: rgba(255, 214, 78, 0.08);
      color: #fff4c2;
      padding: 15px 32px;
      border-radius: 50px;
      font-family: 'Montserrat', sans-serif;
      font-size: 14px;
      letter-spacing: 2px;
      cursor: pointer;
      transition: all 0.3s ease;
      backdrop-filter: blur(8px);
    }

    .open-btn:hover {
      background: rgba(255, 214, 78, 0.18);
      transform: translateY(-3px);
      box-shadow: 0 8px 30px rgba(255, 210, 50, 0.15);
    }

    /* Contenido principal */
    .content {
      position: absolute;
      inset: 0;
      z-index: 20;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      padding: 25px;
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 1.8s ease, transform 1.8s ease;
      pointer-events: none;
    }

    .content.show {
      opacity: 1;
      transform: translateY(0);
    }

    .date {
      font-size: 11px;
      letter-spacing: 5px;
      color: rgba(255, 240, 180, 0.7);
      margin-bottom: 20px;
    }

    .main-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(60px, 13vw, 120px);
      line-height: 0.8;
      font-weight: 500;
      color: #fff3ad;
      text-shadow: 0 0 35px rgba(255, 213, 60, 0.18);
      margin-bottom: 20px;
    }

    .subtitle {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(22px, 5vw, 34px);
      font-style: italic;
      color: rgba(255,255,255,0.9);
      margin-bottom: 30px;
    }

    .message {
      max-width: 700px;
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(21px, 4vw, 29px);
      line-height: 1.45;
      color: rgba(255,255,255,0.92);
      padding: 0 10px;
    }

    .signature {
      margin-top: 30px;
      font-family: 'Cormorant Garamond', serif;
      font-size: 20px;
      font-style: italic;
      color: #ffe99a;
    }

    /* Campo */
    .ground {
      position: absolute;
      bottom: 0;
      left: 0;
      width: 100%;
      height: 24%;
      background:
        linear-gradient(to top, #061007 0%, rgba(7, 20, 13, 0.7) 70%, transparent 100%);
      z-index: 10;
      pointer-events: none;
    }

    /* Flores */
    .flower {
      position: absolute;
      bottom: -10px;
      display: flex;
      flex-direction: column;
      align-items: center;
      transform-origin: bottom center;
      animation: sway 4s ease-in-out infinite;
      z-index: 12;
    }

    .flower-head {
      font-size: clamp(28px, 5vw, 50px);
      filter: drop-shadow(0 0 8px rgba(255, 210, 30, 0.25));
    }

    .stem {
      width: 4px;
      height: 90px;
      background: linear-gradient(to bottom, #5e8a3a, #203c1c);
      border-radius: 5px;
    }

    @keyframes sway {
      0%, 100% {
        transform: rotate(-2deg);
      }
      50% {
        transform: rotate(3deg);
      }
    }

    /* Pétalos */
    .petal {
      position: absolute;
      top: -50px;
      font-size: 18px;
      opacity: 0;
      z-index: 15;
      pointer-events: none;
      animation: fall linear forwards;
    }

    @keyframes fall {
      0% {
        transform: translate3d(0, -50px, 0) rotate(0deg);
        opacity: 0;
      }

      10% {
        opacity: 0.85;
      }

      50% {
        transform: translate3d(70px, 50vh, 0) rotate(180deg);
      }

      100% {
        transform: translate3d(-50px, 110vh, 0) rotate(360deg);
        opacity: 0;
      }
    }

    /* Controles */
    .controls {
      position: absolute;
      right: 20px;
      bottom: 20px;
      z-index: 50;
      display: flex;
      gap: 10px;
    }

    .control-btn {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.25);
      background: rgba(0,0,0,0.25);
      backdrop-filter: blur(8px);
      color: white;
      font-size: 21px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .control-btn:hover {
      transform: scale(1.08);
      background: rgba(255,255,255,0.12);
    }

    /* Girasoles */
    .sunflower {
      position: fixed;
      z-index: 80;
      pointer-events: none;
      font-size: 30px;
      animation: sunflowerRise 6s ease-out forwards;
    }

    @keyframes sunflowerRise {
      0% {
        opacity: 0;
        transform: translateY(0) scale(0.3) rotate(0deg);
      }

      10% {
        opacity: 1;
      }

      50% {
        transform: translateY(-45vh) scale(1.15) rotate(180deg);
      }

      100% {
        opacity: 0;
        transform: translateY(-105vh) scale(0.8) rotate(360deg);
      }
    }

    @media (max-width: 600px) {
      .moon {
        width: 60px;
        height: 60px;
        top: 7%;
        right: 8%;
      }

      .start-flower {
        font-size: 60px;
      }

      .message {
        font-size: 22px;
      }

      .flower-head {
        font-size: 30px;
      }

      .stem {
        height: 65px;
      }

      .controls {
        right: 12px;
        bottom: 12px;
      }
    }
  </style>
</head>

<body>

  <div class="scene">

    <!-- Luna -->
    <div class="moon"></div>

    <!-- Estrellas -->
    <div class="star" style="top:12%;left:18%;animation-delay:.3s"></div>
    <div class="star" style="top:19%;left:35%;animation-delay:1.1s"></div>
    <div class="star" style="top:11%;left:55%;animation-delay:2s"></div>
    <div class="star" style="top:28%;left:72%;animation-delay:.8s"></div>
    <div class="star" style="top:17%;left:82%;animation-delay:1.7s"></div>
    <div class="star" style="top:35%;left:12%;animation-delay:2.4s"></div>
    <div class="star" style="top:31%;left:46%;animation-delay:1.4s"></div>
    <div class="star" style="top:23%;left:62%;animation-delay:.5s"></div>

    <!-- Pantalla inicial -->
    <div class="start-screen" id="startScreen">

      <div class="start-flower">🌼</div>

      <div class="start-title">
        Para ti, Chulis 🌻💛
      </div>

      <div class="start-subtitle">
        21 · 09 · 2026
      </div>

      <button class="open-btn" onclick="openSurprise()">
        🌼 Abrir
      </button>

    </div>

    <!-- Contenido -->
    <div class="content" id="content">

      <div class="date">
        21 · SEPTIEMBRE · 2026
      </div>

      <div class="main-title">
        Chulis
      </div>

      <div class="subtitle">
        te debía unas flores amarillas 🌼
      </div>

      <div class="message">
        Chulis, te quiero hasta en la parte más primitiva de mí,
        donde el corazón no razona y el cuerpo simplemente reconoce
        aquello que le hace bien.
      </div>

      <div class="signature">
        Con mucho amor. 💛
      </div>

    </div>

    <!-- Flores del campo -->
    <div class="flower" style="left:4%;animation-delay:.2s">
      <div class="flower-head">🌼</div>
      <div class="stem"></div>
    </div>

    <div class="flower" style="left:12%;animation-delay:1.1s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:120px"></div>
    </div>

    <div class="flower" style="left:21%;animation-delay:.6s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:80px"></div>
    </div>

    <div class="flower" style="left:31%;animation-delay:1.8s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:110px"></div>
    </div>

    <div class="flower" style="left:42%;animation-delay:.9s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:75px"></div>
    </div>

    <div class="flower" style="left:53%;animation-delay:1.4s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:115px"></div>
    </div>

    <div class="flower" style="left:63%;animation-delay:.3s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:85px"></div>
    </div>

    <div class="flower" style="left:73%;animation-delay:1.7s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:125px"></div>
    </div>

    <div class="flower" style="left:83%;animation-delay:.8s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:90px"></div>
    </div>

    <div class="flower" style="left:93%;animation-delay:1.2s">
      <div class="flower-head">🌼</div>
      <div class="stem" style="height:115px"></div>
    </div>

    <div class="ground"></div>

    <!-- Controles -->
    <div class="controls">

      <button
        class="control-btn"
        onclick="sunflowerExplosion()"
        title="Para ti 🌻">
        🌻
      </button>

      <button
        class="control-btn"
        id="soundButton"
        onclick="toggleSound()"
        title="Música">
        🔊
      </button>

    </div>

  </div>

  <script>

    /* =========================
       MÚSICA
    ========================= */

    let audioContext = null;
    let masterGain = null;
    let soundOn = true;
    let melodyTimer = null;

    const melody = [
      [261.63, 0.45],
      [329.63, 0.45],
      [392.00, 0.65],
      [329.63, 0.40],
      [293.66, 0.40],
      [261.63, 0.70],
      [261.63, 0.40],
      [349.23, 0.40],
      [440.00, 0.65],
      [392.00, 0.40],
      [349.23, 0.40],
      [329.63, 0.75]
    ];

    function playNote(frequency, duration) {

      if (!audioContext || !masterGain || !soundOn) return;

      const oscillator = audioContext.createOscillator();
      const gain = audioContext.createGain();

      oscillator.type = "sine";
      oscillator.frequency.value = frequency;

      gain.gain.setValueAtTime(0, audioContext.currentTime);

      gain.gain.linearRampToValueAtTime(
        0.07,
        audioContext.currentTime + 0.08
      );

      gain.gain.exponentialRampToValueAtTime(
        0.001,
        audioContext.currentTime + duration
      );

      oscillator.connect(gain);
      gain.connect(masterGain);

      oscillator.start();

      oscillator.stop(
        audioContext.currentTime + duration + 0.05
      );
    }

    function playMelody() {

      if (!audioContext || !soundOn) return;

      let delay = 0;

      melody.forEach(([frequency, duration]) => {

        setTimeout(() => {
          playNote(frequency, duration);
        }, delay * 1000);

        delay += duration + 0.06;
      });

      melodyTimer = setTimeout(
        playMelody,
        (delay + 0.5) * 1000
      );
    }

    function startAmbientSound() {

      if (!audioContext) {

        audioContext =
          new (window.AudioContext ||
          window.webkitAudioContext)();

        masterGain =
          audioContext.createGain();

        masterGain.gain.value = 0.8;

        masterGain.connect(
          audioContext.destination
        );
      }

      if (audioContext.state === "suspended") {
        audioContext.resume();
      }

      if (!melodyTimer) {
        playMelody();
      }
    }

    function toggleSound() {

      const button =
        document.getElementById("soundButton");

      if (!audioContext) {
        startAmbientSound();
        return;
      }

      soundOn = !soundOn;

      if (soundOn) {

        masterGain.gain.value = 0.8;

        button.textContent = "🔊";

        if (!melodyTimer) {
          playMelody();
        }

      } else {

        masterGain.gain.value = 0;

        button.textContent = "🔇";

        clearTimeout(melodyTimer);
        melodyTimer = null;
      }
    }


    /* =========================
       ABRIR SORPRESA
    ========================= */

    function openSurprise() {

      const startScreen =
        document.getElementById("startScreen");

      const content =
        document.getElementById("content");

      startScreen.classList.add("hide");

      setTimeout(() => {
        content.classList.add("show");
      }, 700);

      createPetals();

      startAmbientSound();
    }


    /* =========================
       PÉTALOS
    ========================= */

    function createPetals() {

      const symbols = ["🌼", "✨", "🌸"];

      for (let i = 0; i < 35; i++) {

        setTimeout(() => {

          const petal =
            document.createElement("div");

          petal.className = "petal";

          petal.textContent =
            symbols[
              Math.floor(
                Math.random() * symbols.length
              )
            ];

          petal.style.left =
            Math.random() * 100 + "vw";

          petal.style.animationDuration =
            (5 + Math.random() * 5) + "s";

          petal.style.animationDelay =
            Math.random() * 2 + "s";

          petal.style.fontSize =
            (12 + Math.random() * 12) + "px";

          document
            .querySelector(".scene")
            .appendChild(petal);

          setTimeout(() => {
            petal.remove();
          }, 12000);

        }, i * 180);
      }
    }


    /* =========================
       EXPLOSIÓN DE GIRASOLES
    ========================= */

    function sunflowerExplosion() {

      for (let i = 0; i < 32; i++) {

        setTimeout(() => {

          const sunflower =
            document.createElement("div");

          sunflower.className = "sunflower";

          sunflower.textContent = "🌻";

          sunflower.style.left =
            Math.random() * 100 + "vw";

          sunflower.style.bottom =
            (-20 + Math.random() * 20) + "px";

          sunflower.style.fontSize =
            (22 + Math.random() * 25) + "px";

          sunflower.style.animationDuration =
            (4.5 + Math.random() * 2.5) + "s";

          document.body.appendChild(
            sunflower
          );

          setTimeout(() => {
            sunflower.remove();
          }, 7500);

        }, i * 100);

      }
    }

  </script>

</body>
</html>